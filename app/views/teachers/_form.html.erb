<%= form_with(model: teacher) do |form| %>
  <% if teacher.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
        <% teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :is_class_teacher, style: "display: block" %>
    <%= form.check_box :is_class_teacher %>
  </div>
  

  <div>
    <%= form.label :grade_id, style: "display: block" %>
    <%= form.text_field :grade_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
