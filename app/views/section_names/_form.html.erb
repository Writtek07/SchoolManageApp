<%= form_with(model: section_name) do |form| %>
  <% if section_name.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(section_name.errors.count, "error") %> prohibited this section_name from being saved:</h2>

      <ul>
        <% section_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
